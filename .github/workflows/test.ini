name: 编译DIYTVBox

on:
  workflow_dispatch:
    inputs:
      testOption:
        description: '打勾✅添加arm64支持不支持ijk解码'
        required: false
        type: boolean
      add_to_py:
        description: '打勾✅添加PY支持'
        required: false
        type: boolean
      source_branch:
        description: "选择TVBox源分支"
        required: false
        default: "q215613905"
        type: choice
        options:
          - q215613905
          - takagen99

jobs:
  build:
    runs-on: ubuntu-latest
    name: 编译 ${{ inputs.source_branch }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Get env
        working-directory: ${{ github.workspace }}
        run: |
         chmod +x TVBox/${{ inputs.source_branch }}/diy.sh
         source TVBox/${{ inputs.source_branch }}/diy.sh

      - name: Clone project
        working-directory: ${{ github.workspace }}
        run: |
          git clone -b main --depth=1 ${{ env.sourceURL }}.git 
          if [ -f "./TVBoxOSC/app/build.gradle" ]; then
            (
              source ./config.sh
            )
          fi 
          echo "tag=$(git log --date=format:'%Y.%m.%d-%H.%M' -1 --pretty=format:%cd)" >> $GITHUB_ENV
          echo '生成日期完成'
       - name: Clone project
        working-directory: ${{ github.workspace }}
        run: |
          echo '完成'
